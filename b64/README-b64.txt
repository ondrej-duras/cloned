
b64 is utility for
 - encoding files into Base64
 - decoding files from Base64

Install on Windows:
  gcc -o b64.exe b64.c
  strip b64.exe

Install on Linux/Unix:
  gcc -o b64 b64.c
  strip b64

--- end ---

  
